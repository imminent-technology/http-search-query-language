{
  "title": "KQL",
  "slug": "kql",
  "officialDocUrl": "https://learn.microsoft.com/en-us/azure/data-explorer/kusto/query/",
  "mediaType": "None known — KQL text is submitted as a JSON field inside a generic `application/json` body via the Azure Data Explorer/Log Analytics REST APIs.",
  "category": "Analytics/Observability",
  "sources": [
    { "title": "Kusto Query Language (KQL) overview", "publisher": "Microsoft", "url": "https://learn.microsoft.com/en-us/azure/data-explorer/kusto/query/", "date": null }
  ],
  "scores": {
    "expressiveness": { "score": 4, "rationale": "A piped, statement-based language with deep support for text search/parsing, time-series operators, statistical/analytics functions, and geospatial and vector-similarity search, per Microsoft's own description of it as optimized for data analysis." },
    "simplicity": { "score": 4, "rationale": "Data flows through a readable chain of pipe-separated operators (where, count, summarize) applied sequentially to a table, which Microsoft explicitly frames as intuitive enough for newcomers to start writing queries quickly." },
    "flexibility": { "score": 3, "rationale": "Queries are organized against a SQL-like hierarchy of databases, tables, and columns; while it can ingest semi-structured and unstructured data, the query itself still targets defined schema entities rather than being fully schema-free." },
    "communityAndEcosystem": { "score": 4, "rationale": "Used across a wide swath of Microsoft's data/security stack (Azure Data Explorer, Azure Monitor Log Analytics, Microsoft Sentinel, Microsoft Fabric, Microsoft 365 Defender advanced hunting), giving it broad practical reach despite being a single-vendor language." },
    "extensibility": { "score": 3, "rationale": "Supports let statements for reusable expressions/custom logic and a large built-in function/operator library, but the core grammar and operator set are defined and versioned by Microsoft." },
    "transportCompatibility": { "score": 3, "rationale": "Submitted through Kusto's REST API or SDKs/portal UI as a query payload rather than embedded directly in a URL query string; HTTP-accessible but not URL-native like a Lucene-style `q=` parameter." },
    "standardization": { "score": 1, "rationale": "A proprietary Microsoft-defined language with no independent standards body, despite its reuse across many first-party Microsoft products." },
    "security": { "score": 4, "rationale": "The language explicitly distinguishes read-only queries from data/metadata-modifying management commands by requiring the latter to start with a literal `.` character — a documented design choice Microsoft states 'prevents many kinds of security attacks' by making it impossible to embed management commands inside queries." },
    "performance": { "score": 4, "rationale": "Purpose-built for exploring large-scale telemetry, metrics, and log data in cloud-scale big-data stores, with query operator order explicitly affecting both results and performance per Microsoft's own guidance." },
    "orthogonality": { "score": 4, "rationale": "Every query is built from the same uniform building block — a tabular operator connected by pipes, where each operator takes tabular input and produces tabular output — giving the language a small, consistent composition model." }
  },
  "overallScore": 3.4,
  "designQualityScore": 3.6,
  "summary": "KQL is a highly expressive, pipe-based analytics language purpose-built for exploring large-scale telemetry, logs, and security data across Microsoft's Azure/Sentinel/Fabric ecosystem, with a notably deliberate security design (queries vs. dot-prefixed management commands) and a uniform tabular-operator composition model, but it remains a proprietary, non-standardized language accessed via REST API rather than native URL embedding.",
  "example": {
    "scenario": "Retrieve products where category = \"electronics\" and price > 100, sorted by price descending, page 2 of 10 per page.",
    "query": "Products\n| where category == \"electronics\" and price > 100\n| order by price desc\n| serialize rn = row_number()\n| where rn between (11 .. 20)",
    "notes": "KQL has no native OFFSET/SKIP operator — pagination is emulated via serialize + row_number() to number the sorted rows, then filtering to the desired range; this is the documented workaround, not a first-class pagination keyword."
  },
  "evaluatedDate": "2026-09-04"
}
